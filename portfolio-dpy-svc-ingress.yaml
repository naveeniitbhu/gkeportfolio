---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-deployment
  labels:
    app: portfoliowebsite
spec:
  replicas: 1
  selector:
    matchLabels:
      name: portfolio-pod
      app: portfoliowebsite
  template:
    metadata:
      labels:
        name: portfolio-pod
        app: portfoliowebsite
    spec:
      containers:
      - name: portfolio
        image: gcr.io/personal-web-gke/portfolio-app@sha256:ad4782ec7a71207ce2c6cb8033e297db5f3d22041f9df0f54dab1e6a77c5714e
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: portfolio-service
  labels:
    name: portfolio-service
    app: portfoliowebsite
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
  selector:
    name: portfolio-pod
    app: portfoliowebsite
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress-service
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
    cert-manager.io/cluster-issuer: 'letsencrypt-prod'
spec:
  tls:
    - hosts:
        - naveengarg.dev
        - www.naveengarg.dev
      secretName: naveengarg-dev
  rules:
    - host: naveengarg.dev
      http:
        paths:
        - path: /
          backend:
            serviceName: portfolio-service
            servicePort: 80
    - host: www.naveengarg.dev
      http:
        paths:
        - path: /
          backend:
            serviceName: portfolio-service
            servicePort: 80 
---
